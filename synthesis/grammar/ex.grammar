using semantics Ex.Semantics;
using learners Ex.WitnessFunctions;
language Substring;
@complete feature double Score = Ex.RankingScore;

@input string v;
@start string program := v | str | Append(str, program);

string str := v | Substring(v, pos, pos);
int pos := AbsPos(v, k) | RelPos(v, rr);

int k;
Tuple<Regex,Regex> rr;
